#[derive(Hash, PartialEq, Eq, Debug, Copy, Clone)]
pub enum Category {
    Cache,
    Logs,
    RustTargets,
    NodeModules,
    PythonVenvs,
    PythonCaches,
    BunCache,
    CargoGit,
    CargoRegistryCache,
    DiskImages,
    Gradle,
    GradleBuild,
    GradleCache,
    MavenTarget,
    M2Repository,
    PdmBuilds,
    NpmCache,
    ElectronCache,
    VscCache,
    ZigCache,
    ZigBuild,
    SpotifyCache,
    GoCache,
    #[cfg(target_os = "macos")]
    Xcode,
}
